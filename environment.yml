# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: deepvariant-1.2
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bioconda::fastqc
  - bioconda::fastp=0.20.1
  - bioconda::bwa-mem2=2.2.1
  - bioconda::samtools
  - bioconda::multiqc=1.11
  - bioconda::bcftools
